import { PageIntro } from "@/components/page-intro";

export const metadata = { title: "Contact" };

export default function ContactPage() {
  return (
    <PageIntro
      description="Professional contact options will be available here."
      eyebrow="Connect"
      title="Contact"
    />
  );
}
